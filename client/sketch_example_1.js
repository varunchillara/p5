// set up block runs once and only once!!
function setup() {
  createCanvas(1280, 720);
  background(220);
  // square(50, 50, 80);
  // frameRate(10);
}

//the draw block runs repeatadly and is used for animations
function draw() {
  //comment this back in to see a dynamic animation!!
  // if (mouseIsPressed) {
  //   square(mouseX, mouseY, 90);
  // } else {
  //   circle(mouseX, mouseY, 90);
  // }
}